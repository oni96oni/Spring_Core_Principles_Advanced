예제 만들기
    프로젝트 생성

    예제 프로젝트 만들기 - V0
        @RequireArgsConstructor 가 생성자를 대신 만들어준다!
        그런데 생성자가 하나만 있으면 자동으로 @Autowired가 붙는다.
        그래서 자동으로 생성자 주입이 되는것

        @RestController = @Controller + @ResponseBody

    로그 추적기 - 요구사항
        모든 PUBLIC 메서드의 호출과 응답 정보를 로그로 출력
        애플리케이션의 흐름을 변경하면 안됨
            로그를 남긴다고 해서 비즈니스 로직의 동작에 영향을 주면 안됨
        메서드 호출에 걸린 시간
        정상 흐름과 예외 흐름 구분
        예외 발생시 예외 정보가 남아야 함
        메서드 호출의 깊이 표현
        HTTP 요청을 구분
            HTTP 요청 단위로 특정 ID를 남겨서 어떤 HTTP 요청에서 시작된 것인지 명확하게 구분이 가능해야함
            트랜잭션 ID (DB 트랜잭션X), 여기서는 하나의 HTTP 요청이 시작해서 끝날 때 까지를 하나의 트랜잭션이라 함

    로그 추적기 V1 - 프로토타입 개발
        테스트는 자동으로 검증하는 과정이 필요! 결과를 따로 콘솔로 볼 필요가 없어야 한다.
        응답값이 없는 경우 자동검증 하려면 여러 테스트 기법이 필요하다.

